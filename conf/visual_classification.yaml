defaults:
  - hydra: no_logging
runner:
  # seed: 0
  device: cuda:0
  progress: True
  print_to_term: True
  log_mlflow: True
  exp_name: cifar10
  test_every_epoch: True
  val: ${dataset.val}
  inverter:
    _name: 'undo_norm'
    mean: ${transforms.test.mean}
    std: ${transforms.test.std}
  save_images: 4
  # dry_run: False